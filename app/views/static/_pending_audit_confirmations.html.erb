<% pending_audit_confirmations.each do |pending_audit_confirmation|  %>

<%= link_to "#{pending_audit_confirmation.start_date}", confirm_audit_log_path(pending_audit_confirmation), class: "btn btn-primary btn-block  btn-lg", id: "confirm_#{pending_audit_confirmation.id}", data: { confirm: 'Are you sure you did not perform any overtime? Please Confirm?'} %>

<% end %>
